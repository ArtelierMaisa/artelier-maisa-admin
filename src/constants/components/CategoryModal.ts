import { CategoryModalProps } from '../../@types';

export const categoryModalTitles: Record<
  CategoryModalProps['variant'],
  string
> = {
  add: 'Adicionar Nova Categoria?',
  edit: 'Editar Categoria?',
};
